import type { ThemeState } from '../types'
export default function ThemeCustomizer({value,onChange}:{value:ThemeState;onChange:(t:ThemeState)=>void}){return <div className="theme-controls glass-card"><span>Theme</span><label title="Accent color"><input type="color" value={value.accent} onChange={e=>onChange({...value,accent:e.target.value})}/></label><select aria-label="Font size" value={value.fontSize} onChange={e=>onChange({...value,fontSize:+e.target.value})}><option value="14">Small</option><option value="16">Normal</option><option value="18">Large</option></select><button className={`mode-btn ${value.mode==='light'?'active':''}`} onClick={()=>onChange({...value,mode:'light'})}>Light</button><button className={`mode-btn ${value.mode==='dark'?'active':''}`} onClick={()=>onChange({...value,mode:'dark'})}>Dark</button></div>}
